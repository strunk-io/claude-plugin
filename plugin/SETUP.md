# Setting up Strunk

The setup guidance lives in the `strunk-setup` skill, at `skills/strunk-setup/SKILL.md`, so that Claude
actually loads it: a file at the plugin root is not picked up as a component.

This file keeps the name the plugin directory submission guide refers to. It holds no separate copy of
the steps, so the two cannot drift apart.
